{{- if .Values.godelApi.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "godel.fullname" . }}-api
  labels:
    {{- include "godel.apiLabels" . | nindent 4 }}
  {{- with .Values.godelApi.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.godelApi.ingress.className }}
  ingressClassName: {{ .Values.godelApi.ingress.className }}
  {{- end }}
  {{- if .Values.godelApi.ingress.tls }}
  tls:
    {{- range .Values.godelApi.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.godelApi.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "godel.fullname" $ }}-api
                port:
                  number: {{ $.Values.godelApi.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
