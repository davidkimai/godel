apiVersion: v1
kind: Namespace
metadata:
  name: dash
  labels:
    name: dash
    app.kubernetes.io/name: dash
    app.kubernetes.io/component: namespace
    app.kubernetes.io/part-of: dash-platform
  annotations:
    description: "Dash Agent Orchestration Platform namespace"
---
# Resource quota for the namespace
apiVersion: v1
kind: ResourceQuota
metadata:
  name: dash-quota
  namespace: dash
spec:
  hard:
    requests.cpu: "20"
    requests.memory: 40Gi
    limits.cpu: "40"
    limits.memory: 80Gi
    pods: "100"
    services: "20"
    persistentvolumeclaims: "10"
---
# LimitRange for default resource constraints
apiVersion: v1
kind: LimitRange
metadata:
  name: dash-limits
  namespace: dash
spec:
  limits:
    - default:
        cpu: "1"
        memory: 1Gi
      defaultRequest:
        cpu: 100m
        memory: 256Mi
      max:
        cpu: "4"
        memory: 8Gi
      min:
        cpu: 50m
        memory: 128Mi
      type: Container
