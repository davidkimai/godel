apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: godel-api-hpa
  namespace: godel
  labels:
    app.kubernetes.io/name: godel-api-hpa
    app.kubernetes.io/component: autoscaler
    app.kubernetes.io/part-of: godel-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: godel-api
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
    - type: Pods
      pods:
        metric:
          name: godel_active_agents
        target:
          type: AverageValue
          averageValue: "10"
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
        - type: Pods
          value: 4
          periodSeconds: 15
      selectPolicy: Max
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: godel-godelboard-hpa
  namespace: godel
  labels:
    app.kubernetes.io/name: godel-godelboard-hpa
    app.kubernetes.io/component: autoscaler
    app.kubernetes.io/part-of: godel-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: godel-godelboard
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Percent
          value: 50
          periodSeconds: 60
---
# Custom Metrics API configuration for pod metrics
apiVersion: v1
kind: ConfigMap
metadata:
  name: godel-custom-metrics
  namespace: godel
data:
  # Custom metrics for HPA based on application metrics
  godel_active_agents: |
    seriesQuery: 'godel_active_agents{namespace="godel"}'
    resources:
      overrides:
        namespace:
          resource: namespace
        pod:
          resource: pod
    name:
      matches: "^(.*)_agents$"
      as: "${1}_agents"
    metricsQuery: 'sum(<<.Series>>{<<.LabelMatchers>>}) by (<<.GroupBy>>)'
