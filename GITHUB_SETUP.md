# GitHub Repository Setup

**Repo:** https://github.com/davidkimai/dash  
**Status:** ✅ Initialized and pushed

## Commands

```bash
# Push changes
git add .
git commit -m "Your message"
git push

# Pull latest
git pull origin main
```

## Workflow

1. Work in `/Users/jasontang/clawd/projects/mission-control/`
2. Commit changes: `git add . && git commit -m "description"`
3. Push to GitHub: `git push`
4. Pull on other machines: `git pull`

## CI/CD (Future)

Add GitHub Actions for:
- `npm run build` on every push
- `npm test` on every push
- Linting checks
